# Archived Frameworks

This directory contains historical framework versions preserved for reference.

- These packages are not maintained and are not supported for active development.
- They are intentionally archived and no longer live at the repository root.
- Use the canonical package `dare_framework/` for current development.

If you need to inspect or run archived code, you must explicitly adjust your `PYTHONPATH` or tooling to point into `archive/frameworks/`.
